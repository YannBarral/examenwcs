{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste Achat</h1>

    <table>
        <thead>
            <tr>

            </tr>
        </thead>
        <tbody>
        {% for achat in achats %}
            <tr>
                <td><a href="{{ path('achat_show', { 'id': achat.id }) }}">{{ achat.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('achat_show', { 'id': achat.id }) }}">Montrer</a>
                        </li>
                        <li>
                            <a href="{{ path('achat_edit', { 'id': achat.id }) }}">Modifier</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('achat_new') }}">Créer un nouveau produit</a>
        </li>
    </ul>
{% endblock %}
