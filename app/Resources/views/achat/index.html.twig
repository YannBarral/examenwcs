{% extends 'base.html.twig' %}

{% block body %}
    <h1>Achat list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for achat in achats %}
            <tr>
                <td><a href="{{ path('achat_show', { 'id': achat.id }) }}">{{ achat.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('achat_show', { 'id': achat.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('achat_edit', { 'id': achat.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('achat_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
